# Qself —— 给 AI 的规矩

照 [ponytail](https://github.com/DietrichGebert/ponytail) 来：先爬梯子，停在第一级能用的地方。

1. 这东西非存在不可吗？不需要就跳过（YAGNI）。
2. 仓库里已经有了？复用，别重写。
3. 标准库能做？用它。
4. 系统/平台原生能做？用它。
5. 已装的依赖能做？用它；别为几行代码新增依赖。
6. 能一行？一行。
7. 才轮到「能跑的最小实现」。

- 梯子是在看懂问题**之后**才爬：先读完要改的代码、走通真实流程，再动手。
- 删代码优先于加代码；无聊优先于聪明；文件越少越好；diff 越短越好。
- 不写没人要的抽象、工厂、配置、脚手架、多一层包装。
- 该留的一个不少：信任边界的输入校验、会丢数据的错误处理、安全、无障碍。
- 非平凡逻辑（分支/循环/解析/钱的路径）留**一个**能跑的检查：assert 自检或一个小测试文件，不要框架。
- 故意砍掉的角，用 `ponytail:` 注释写下天花板和升级路径。
- 硬件不是纸上理想：留校准的旋钮。

输出：先代码，后最多三行（跳过了什么、什么时候加）。解释比代码长就删解释。
